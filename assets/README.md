# Visual assets

All visuals are original deterministic SVG files generated for this repository. They use system fonts, contain no third-party photos, and include `role=img` plus an accessible label. Source generation logic lives in `scripts/generate_visuals.py`.
